import { FC } from 'react';
import { GameSettingsBehaviorProps } from './GameSettingsTypes';
import GameSettingsTemplate from './GameSettingsTemplate';

const GameSettingsBehavior: FC<GameSettingsBehaviorProps> = () => (
  <GameSettingsTemplate />
);

export default GameSettingsBehavior;
